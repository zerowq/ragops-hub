# RAGOps Hub 产品指南

系统支持 PDF、Word、Markdown 和纯文本文件。文档上传后会经过文本解析、结构化切分、Embedding 和向量入库。

检索阶段会先执行租户和部门权限过滤，再融合向量检索与 BM25 关键词检索结果，最后进行轻量重排。回答会携带文档标题、来源和 Chunk 位置，方便用户核验。

客服 Agent 支持企业知识问答、订单查询和工单创建。工单创建属于有副作用操作，必须由用户二次确认后执行。
